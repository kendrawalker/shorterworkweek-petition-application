<!-- description -->

<title>Petition Supporters</title>
<link rel="stylesheet" href="/stylesheet.css"/>
<body>
    {{>menu}}
    <div id="background">
        <h1>We support the movement to shorten the workweek and lengthen our weekends.</h1>
        <h4>The people below have signed this petition in support of the official creation of 4 day work weeks and 3 day weekends.</h4>
        <div id="names">
            {{#signers}}
                <div class="supporter-info">
                    <div class="supp-names">{{first_name}} {{last_name}}
                        <a class="peanuts">{{age}}</a>
                        <a class="peanuts city-link" href="/supporters/{{city}}">{{city}}</a>
                        <a class="peanuts homepage-link" href="{{homepage}}">{{homepage}}</a>
                    </div>
                </div>
            {{/signers}}
        </div>
    </div>
</body>
